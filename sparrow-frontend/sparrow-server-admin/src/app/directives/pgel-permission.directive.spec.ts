import { PgelPermissionDirective } from './pgel-permission.directive';

describe('PgelPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new PgelPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
